[project]
name = "code-golf"
version = "0.1.0"
description = "Utilities for experimenting with code golf ideas"
requires-python = ">=3.12"
dependencies = [
    "google-genai",
]

[dependency-groups]
dev = []

